{
  "skill_name": "clerk-vue-patterns",
  "evals": [
    {
      "id": 1,
      "prompt": "i'm adding clerk to my nuxt 3 app. set up the module and protect the /dashboard route so unauthenticated users are redirected to /sign-in.",
      "expected_output": "Adds @clerk/nuxt to modules in nuxt.config.ts, creates Nuxt route middleware that checks auth and redirects",
      "scaffold": "vue-basic-auth",
      "expectations": [
        "Adds @clerk/nuxt to the modules array in nuxt.config.ts",
        "Creates a route middleware that checks authentication",
        "Uses useAuth composable inside the middleware",
        "Redirects unauthenticated users to /sign-in via navigateTo",
        "Applies the middleware to the /dashboard route"
      ]
    },
    {
      "id": 2,
      "prompt": "show me how to use clerk composables in a vue 3 component to display the user's name and a sign-out button.",
      "expected_output": "Uses useAuth and useUser composables from @clerk/vue, shows user.firstName, calls signOut()",
      "scaffold": "vue-basic-auth",
      "expectations": [
        "Imports useAuth and useUser from @clerk/vue",
        "Uses isSignedIn from useAuth to conditionally render",
        "Accesses user.firstName or user.fullName from useUser",
        "Calls signOut() from useAuth on button click",
        "Handles the case where user is not signed in"
      ]
    },
    {
      "id": 3,
      "prompt": "my nuxt app has a server API route /api/user-data that should only work for authenticated users. implement the server-side auth check.",
      "expected_output": "Uses getAuth from @clerk/nuxt/server in the event handler, throws 401 when userId is falsy",
      "scaffold": "vue-basic-auth",
      "expectations": [
        "Imports getAuth from @clerk/nuxt/server",
        "Calls getAuth(event) to get userId",
        "Throws createError({ statusCode: 401 }) when userId is falsy",
        "Uses userId in the data fetching logic",
        "Does NOT use client-side composables in the server route"
      ]
    },
    {
      "id": 4,
      "prompt": "i want to build a custom sign-in form in vue using clerk's useSignIn composable. show the email/password flow.",
      "expected_output": "Uses useSignIn composable, calls signIn.create with identifier and password, sets active session after success",
      "scaffold": "vue-basic-auth",
      "expectations": [
        "Imports useSignIn from @clerk/vue",
        "Destructures signIn and setActive from useSignIn",
        "Calls signIn.create({ identifier, password }) on form submit",
        "Calls setActive({ session: signIn.createdSessionId }) after successful sign-in",
        "Handles errors from the sign-in attempt"
      ]
    },
    {
      "id": 5,
      "prompt": "i need to add a global nuxt server middleware that checks auth on all protected routes and redirects to /sign-in. use clerkMiddleware from @clerk/nuxt.",
      "expected_output": "Creates server/middleware/clerk.ts with clerkMiddleware that checks H3Event context for auth",
      "scaffold": "vue-basic-auth",
      "expectations": [
        "Creates server/middleware/clerk.ts",
        "Uses clerkMiddleware from @clerk/nuxt/server",
        "Exports the result as default",
        "Can access auth state via event.context.auth",
        "Does NOT create the middleware in middleware/ (that is for route middleware, not server middleware)"
      ]
    },
    {
      "id": 6,
      "prompt": "i want a pinia store that syncs with clerk auth state. the store should expose isAuthenticated and userId, and update reactively when the user signs in or out.",
      "expected_output": "Creates a Pinia store that uses useAuth composable, exposes isSignedIn and userId as computed values",
      "scaffold": "vue-basic-auth",
      "expectations": [
        "Creates a Pinia store using defineStore",
        "Calls useAuth inside the store setup function",
        "Exposes isSignedIn (or renamed isAuthenticated) from useAuth",
        "Exposes userId from useAuth",
        "Does NOT duplicate auth state — wraps useAuth reactivity rather than copying it"
      ]
    }
  ]
}
