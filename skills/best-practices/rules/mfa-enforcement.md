---
title: Enforce MFA When Enabled
impact: HIGH
impactDescription: Prevents access bypass when MFA is configured
tags: mfa, totp, sms, backup-codes, step-up
---

## Enforce MFA When Enabled

<!-- TODO: Document TOTP enrollment flow, SMS fallback, backup codes generation
     and redemption. Show how to check MFA status in sessionClaims and enforce
     step-up auth in custom flows. Cover the difference between MFA being
     available vs required. -->
